import Image from "next/image";
import Link from "next/link";
import { ArrowRight, BookOpen, Compass } from "lucide-react";
import { organs } from "@/components/anatomy/organ-data";

export default function LibraryPage() {
  return <main className="min-h-screen bg-[#f8f1e8] px-5 py-6 text-[#332d29] sm:px-9 lg:px-14">
    <header className="mx-auto flex max-w-7xl items-center justify-between gap-5"><Link className="font-[family-name:var(--font-serif)] text-4xl tracking-[-.05em]" href="/">Corpus<span className="ml-1 align-top font-[family-name:var(--font-sans)] text-sm text-[#eb7c6b]">✦</span></Link><nav className="hidden items-center gap-5 text-sm text-[#675d56] sm:flex"><Link href="/anatomy">Explore</Link><Link className="font-semibold text-[#3f3530]" href="/library">Library</Link><Link href="/guide">Guide</Link></nav><Link className="rounded-full border border-[#b88c76]/35 bg-white/45 px-4 py-2 text-sm font-medium" href="/anatomy">Open explorer</Link></header>
    <section className="mx-auto max-w-7xl py-14 sm:py-20"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#a46a5f]">Atlas library</p><h1 className="mt-4 max-w-3xl font-[family-name:var(--font-serif)] text-5xl leading-[.95] tracking-[-.05em] sm:text-7xl">Choose a specimen.<br /><em className="font-normal text-[#8d6bcc]">Start seeing relationships.</em></h1><p className="mt-6 max-w-2xl text-lg leading-8 text-[#625850]">Every available model opens directly in the 3D explorer with landmarks, spatial stories, and concise reviewed orientation notes.</p></section>
    <section className="mx-auto grid max-w-7xl gap-5 pb-16 sm:grid-cols-2 lg:grid-cols-3">{organs.map((organ) => <article className="group rounded-[1.7rem] border border-[#b88c76]/20 bg-white/40 p-5 shadow-[0_16px_35px_-28px_rgba(87,62,43,.4)]" key={organ.id}><div className="flex items-start justify-between gap-3"><Image alt="" className="h-20 w-20 rounded-full object-cover mix-blend-multiply" height={96} src={`/anatomy/${organ.id}/thumb.webp`} width={96} /><span className="rounded-full bg-[#f4e7dc] px-3 py-1 text-[10px] font-bold uppercase tracking-[.1em] text-[#8b6657]">{organ.region}</span></div><h2 className="mt-6 font-[family-name:var(--font-serif)] text-4xl tracking-[-.04em]">{organ.name}</h2><p className="mt-1 text-sm text-[#796e66]">{organ.system}</p><p className="mt-4 min-h-12 text-sm leading-6 text-[#625850]">{organ.description}</p><div className="mt-5 flex flex-wrap gap-2"><Link className="inline-flex items-center gap-1 rounded-full bg-[#3f3530] px-3 py-2 text-xs font-semibold text-white" href={`/anatomy?organ=${organ.id}`}>Explore <ArrowRight size={13} /></Link><Link className="inline-flex items-center gap-1 rounded-full border border-[#b88c76]/30 bg-white/45 px-3 py-2 text-xs font-semibold text-[#5e5048]" href={`/anatomy?organ=${organ.id}&mode=guide`}><BookOpen size={13} /> Guide</Link></div></article>)}</section>
    <footer className="mx-auto flex max-w-7xl items-center gap-2 border-t border-[#b88c76]/20 py-7 text-sm text-[#74675f]"><Compass size={15} /> {organs.length} interactive specimens currently available.</footer>
  </main>;
}
